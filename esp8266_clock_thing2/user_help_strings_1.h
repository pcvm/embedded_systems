"\n\rt - specify alternative timeserver (e.g. t192.168.1.10 which helps on a standalone network)\n\r\
g - specify alternative PB switch input line (e.g. g10 if original g16 input is damaged)\n\r\
h - specify the upgrade server (for testing use)\n\r\
d - set dimming with Level 0..15, Start hour, End hour (e.g. d0,22,6 means lights low 10pm-6am)\n\r\
L - specify LED display type (e.g. L0 for 7segment, L1 for DOT MATRIX, L1x for DOT MATRIX/rotated x=0..3 *90deg; typically leave as-is)\n\r\
H - select 12 or 24 hour display (e.g. H1 for 12 hour mode, H2 for 24 hour mode)\n\r\
G - select GPS time source (e.g. G1 for GPS time using attached GPS receiver; typically leave as-is)\n\r\
"
